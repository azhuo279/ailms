import type { Metadata } from "next";
import { RouteStub } from "@/components/shared/route-stub";

export const metadata: Metadata = {
  title: "Alerts · AiLMS",
};

export default function AlertsPage() {
  return <RouteStub title="Alerts" />;
}
